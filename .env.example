# Kraken API
KRAKEN_API_KEY=
KRAKEN_SECRET_KEY=

# Anthropic (Claude) — required if ai.provider = "anthropic"
ANTHROPIC_API_KEY=

# Google Vertex — required if ai.provider = "vertex"
# Mount key file into container, then set path inside container:
GOOGLE_APPLICATION_CREDENTIALS=/app/keys/vertex-sa.json

# Telegram Bot
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# CoinGecko — optional, improves rate limits for global market data
COINGECKO_API_KEY=

# Data API (required if api.enabled = true)
API_KEY=
