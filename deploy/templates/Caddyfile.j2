# Trading Brain API reverse proxy
# To enable HTTPS: replace the line below with your domain (e.g., api.example.com)
:80 {
	reverse_proxy localhost:8080
}

# Grafana dashboard
:3000 {
	reverse_proxy localhost:3000
}
