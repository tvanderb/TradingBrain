all:
  children:
    trading:
      hosts:
        trading-brain:
          ansible_host: ""           # Remote IP or hostname
          ansible_user: ""           # SSH user
          ansible_ssh_private_key_file: ""  # Path to SSH private key
          ansible_become: true

          # Secrets — fill in or use ansible-vault
          kraken_api_key: ""
          kraken_secret_key: ""
          anthropic_api_key: ""            # Used when ai.provider = "anthropic"
          telegram_bot_token: ""
          telegram_chat_id: ""
          api_key: ""
          grafana_admin_password: ""

          # Google Vertex AI (used when ai.provider = "vertex")
          # vertex_sa_key_file: "keys/vertex-sa.json"  # Path to service account key (relative to deploy/)
